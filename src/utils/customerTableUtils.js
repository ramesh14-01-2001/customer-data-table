import { AMOUNT, DATE, NAME, STATUS } from "../constants/constants";

export const columns = [
    NAME.toLowerCase(),
    DATE.toLowerCase(),
    STATUS.toLowerCase(),
    AMOUNT.toLowerCase()
];

export const headers = [
    NAME,
    DATE,
    STATUS,
    AMOUNT
];